#!/bin/sh
# A stand-in secret helper: every reference is present, and a launch gets a
# placeholder in place of the secret. It never holds a real one.
if [ "$1" = --check ]; then exit 0; fi
while [ $# -gt 0 ] && [ "$1" != -- ]; do export "${1%%=*}=resolved-placeholder"; shift; done
shift
exec "$@"
